<resources>

    <!-- Base application theme. -->
    <style name="MileageTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@color/mileage_accent_material_light</item>
    </style>

    <style name="rwk_button" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">@color/mileage_accent_material_light</item>
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:layout_marginTop">3dp</item>
    </style>

    <!-- Spacing for delivery list items -->
    <style name="list_spacing">
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:layout_marginRight">5dp</item>
        <item name="android:layout_marginLeft">3dp</item>
    </style>

</resources>
